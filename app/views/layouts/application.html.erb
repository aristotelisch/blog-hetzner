<!DOCTYPE html>
<html>
<head>
  <title>Blog</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="container">
    <header>
    <h1>Learning Rails Blog</h1>
    <nav>
    <ul>
      <li><%= link_to "Blog", root_path %></li>
      <% if @current_user %>
        <li>Signed in as <%= @current_user.username %> <%= link_to "Sign Out", session_path, method: :delete if @current_user  %></li>
      <% else %>
        <li><%= link_to "Sign In", new_session_path unless @current_user  %></li>
      <% end %>
    </ul>
    </nav>

    <% flash.each do |name, message| %>
      <%= content_tag :div, message, class: name %>
    <% end %>
    </header>
<%= yield %>

</div>
  <script>
    (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
        (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
    })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

    ga('create', 'UA-16061280-4', 'happybit.eu');
    ga('send', 'pageview');

  </script>
</body>
</html>
